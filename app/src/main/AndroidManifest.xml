<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.uk.bawmpt.unimyportalapp">

    <application
        android:name=".util.UserApi"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UniMyPortalApp">
        <activity android:name=".views.LibraryMyReservationActivity"></activity>
        <activity android:name=".views.LibrarySearchCatalogActivity" />
        <activity android:name=".views.PostBookForSaleActivity" />
        <activity android:name=".views.MessagesActivity" />
        <activity android:name=".views.ActivitiesActivity" />
        <activity android:name=".views.ForumActivity" />
        <activity android:name=".views.BookSaleListActivity" />
        <activity android:name=".views.LibraryActivity" />
        <activity android:name=".views.FloorPlanActivity" />
        <activity android:name=".views.TimeTableActivity" />
        <activity android:name=".views.DashboardActivity" />
        <activity android:name=".views.CreateAccountActivity" />
        <activity android:name=".views.LoginActivity" />
        <activity android:name=".views.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
</manifest>